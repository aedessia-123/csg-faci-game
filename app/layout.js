import "./globals.css";

export const metadata = {
  title: "The Facilitator Matrix — Game 1",
  description: "L0 Pre-Facilitator readiness scenarios",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
